package com.wbeauty.crm.feature.customer.data

/**
 * اتصال بین منبع داده مشتری و Repository.
 *
 * این کلاس پایه برای تکمیل جریان:
 * UI -> ViewModel -> UseCase -> Repository -> DataSource
 * ایجاد شده است.
 */
class CustomerRepositoryConnection {
    // در مرحله اتصال کامل Room پیاده‌سازی می‌شود.
}
