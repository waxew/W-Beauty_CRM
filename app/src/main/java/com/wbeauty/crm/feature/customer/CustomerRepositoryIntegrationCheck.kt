package com.wbeauty.crm.feature.customer

/**
 * بررسی یکپارچگی مسیر مشتری.
 * این کلاس نقطه ثبت مرحله اتصال UI به لایه داده است.
 */
object CustomerRepositoryIntegrationCheck {
    const val STATUS = "UI -> ViewModel -> UseCase -> Repository -> Room"
}
